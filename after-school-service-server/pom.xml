<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.afterschool</groupId>
    <artifactId>after-school-service-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>after-school-service-server</name>
    <description>中小学课后服务选课与教务监管平台后端</description>

    <properties>
        <java.version>21</java.version>
        <mybatis-spring-boot.version>4.0.1</mybatis-spring-boot.version>
        <springdoc.version>3.0.3</springdoc.version>
        <dependency-check.version>12.2.2</dependency-check.version>
        <tomcat.version>11.0.24</tomcat.version>
        <jackson-2-bom.version>2.21.5</jackson-2-bom.version>
        <log4j2.version>2.25.5</log4j2.version>
        <swagger-ui.version>5.32.13</swagger-ui.version>
        <!--
            The normal artifact is the only artifact eligible for a production
            release. Demo seed migrations are opt-in because Flyway resources
            are otherwise packaged by default with Spring Boot applications.
        -->
        <demo.resource.exclusion>db/demo/**</demo.resource.exclusion>
        <demo.profile.resource.exclusion>application-demo.yml</demo.profile.resource.exclusion>
        <demo.component.exclusion>com/afterschool/platform/demo/**</demo.component.exclusion>
        <artifact.suffix></artifact.suffix>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis-spring-boot.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>swagger-ui</artifactId>
            <version>${swagger-ui.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}${artifact.suffix}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>${demo.resource.exclusion}</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <failIfNoTests>true</failIfNoTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <!--
                        Filter again at archive time so an incremental normal
                        package cannot reuse db/demo files left in target/
                        by an earlier demo-artifact build.
                    -->
                    <excludes>
                        <exclude>${demo.resource.exclusion}</exclude>
                        <exclude>${demo.profile.resource.exclusion}</exclude>
                        <exclude>${demo.component.exclusion}</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>demo-artifact</id>
            <properties>
                <!-- Deliberately unmatched: this profile packages the complete demo payload. -->
                <demo.resource.exclusion>__demo_artifact_includes_all_resources__</demo.resource.exclusion>
                <demo.profile.resource.exclusion>__demo_artifact_includes_demo_profile__</demo.profile.resource.exclusion>
                <demo.component.exclusion>__demo_artifact_includes_demo_components__</demo.component.exclusion>
                <artifact.suffix>-demo</artifact.suffix>
            </properties>
        </profile>
        <profile>
            <id>security</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>${dependency-check.version}</version>
                        <configuration>
                            <failBuildOnCVSS>7.0</failBuildOnCVSS>
                            <failOnError>true</failOnError>
                            <ossIndexAnalyzerEnabled>false</ossIndexAnalyzerEnabled>
                            <formats>
                                <format>HTML</format>
                                <format>JSON</format>
                            </formats>
                            <nvdApiKeyEnvironmentVariable>NVD_API_KEY</nvdApiKeyEnvironmentVariable>
                        </configuration>
                        <executions>
                            <execution>
                                <id>known-vulnerability-gate</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
