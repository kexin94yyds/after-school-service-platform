<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.afterschool.platform.auth.BootstrapAdminMapper">
    <select id="countUsername" resultType="int">
        SELECT COUNT(*)
        FROM sys_user
        WHERE username = #{username}
    </select>

    <insert id="insertRegulator">
        INSERT INTO sys_user (
            school_id, role_id, username, password_hash, display_name, enabled
        ) VALUES (
            NULL,
            (SELECT id FROM sys_role WHERE role_code = 'REGULATOR'),
            #{username},
            #{passwordHash},
            #{displayName},
            TRUE
        )
    </insert>
</mapper>
